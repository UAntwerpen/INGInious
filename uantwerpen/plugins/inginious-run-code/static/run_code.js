function load_input_run_code(submissionid, key, input) {
    load_input_code(submissionid, key, input);
}

function studio_init_template_run_code(well, pid, problem)
{
    studio_init_template_code(well, pid, problem);
}

function load_feedback_run_code(key, content) {
    load_feedback_code(key, content);
}